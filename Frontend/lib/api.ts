/**
 * All backend API endpoint paths in one place.
 * Only paths — no base URL logic here.
 * Add new objects as features grow (blogs, categories, etc.)
 */
export const API = {
  auth: {
    login: '/auth/login',
  },
} ;
